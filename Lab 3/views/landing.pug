doctype html
html(lang="en")
    head
        title MVC Ex Solution Landing Page
    body
        p SER421 MVC Ex Landing Page
        form(action="/landing", method="POST")
            p 
                | Username: 
                input(type="text", name="username", required, pattern="[a-zA-Z]{1,}", value= username)
            p
                input(type="submit", name="action", value="survey")
                | 
                input(type="submit", name="action", value="match")